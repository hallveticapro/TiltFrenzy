services:
  tilted:
    image: ${TILTED_IMAGE:-tilted:latest}
    build:
      context: .
    container_name: tilted
    ports:
      - "8080:8080"
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
